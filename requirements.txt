# 專案整體依賴（供 pipeline.py 與 webapp/app.py 使用）
#
# 安裝方式：
#   pip install -r requirements.txt
#
# 說明：
# - web3: 從鏈上取得合約 bytecode（pipeline.py）
# - flask: 提供 Web 介面（webapp/app.py）
# - torch / torch-geometric / numpy: GNN 模型推論與特徵處理（GNN/detect.py 等）

web3>=6.0.0
flask>=2.0
torch>=1.12.0
torch-geometric>=2.0.0
numpy>=1.20.0

